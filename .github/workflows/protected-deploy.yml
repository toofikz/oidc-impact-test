name: Protected Deploy Test
on: [workflow_dispatch]
permissions:
  id-token: write
  contents: read
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: prod-protected
    steps:
      - name: Deploy step
        run: echo "Deployed to prod-protected"
